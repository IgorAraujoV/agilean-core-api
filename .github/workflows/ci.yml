name: CI

on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.SUBMODULE_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      # Build submodule first (no lock file, so npm install)
      - name: Install & build agilean (submodule)
        working-directory: agilean
        run: npm install && npm run build

      # Then server
      - run: npm ci
      - run: npm run build
      - run: npm test -- --testPathIgnorePatterns='Benchmark'
